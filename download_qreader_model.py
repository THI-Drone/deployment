# This script makes sure the QReader (nano) model is downloaded

# Import the QReader class from the qreader module
from qreader import QReader

# Download the model by calling the QReader class
qreader = QReader(model_size = 'n')